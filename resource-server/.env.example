# ============================================================
# RESOURCE SERVER CONFIGURATION
# ============================================================
# Copy this file to .env and fill in your values
# ============================================================

# Express Server
PORT=3001

# ============================================================
# OAuth2/OIDC Configuration
# ============================================================

# Keycloak Issuer URL
# Used for validating the 'iss' claim in tokens
OAUTH_ISSUER=http://localhost:8080/realms/oauth2-learning

# JWKS URI
# Endpoint to fetch public keys for JWT verification
OAUTH_JWKS_URI=http://localhost:8080/realms/oauth2-learning/protocol/openid-connect/certs

# Token Introspection (optional)
# Used for checking if tokens are revoked
OAUTH_INTROSPECTION_ENDPOINT=http://localhost:8080/realms/oauth2-learning/protocol/openid-connect/token/introspect

# Resource Server Credentials (for introspection)
# Create a client in Keycloak with Service Accounts enabled
OAUTH_RESOURCE_SERVER_CLIENT_ID=resource-server
OAUTH_RESOURCE_SERVER_CLIENT_SECRET=your-resource-server-secret
