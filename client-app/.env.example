# ============================================================
# CLIENT APPLICATION CONFIGURATION
# ============================================================
# Copy this file to .env and fill in your values
# ============================================================

# Express Server
PORT=3000
SESSION_SECRET=change-this-to-a-secure-random-string

# ============================================================
# OAuth2/OIDC Configuration
# ============================================================
# These values must match your Keycloak configuration

# Keycloak Realm URL (Issuer)
# Format: http://localhost:8080/realms/{realm-name}
OAUTH_ISSUER=http://localhost:8080/realms/oauth2-learning

# Client Credentials
# Get these from Keycloak Admin Console:
# Clients > express-client > Credentials
OAUTH_CLIENT_ID=express-client
OAUTH_CLIENT_SECRET=your-client-secret-from-keycloak

# Redirect URI
# Must exactly match what's configured in Keycloak client settings
OAUTH_REDIRECT_URI=http://localhost:3000/auth/callback

# Post-logout redirect URI
OAUTH_POST_LOGOUT_REDIRECT_URI=http://localhost:3000

# Scopes to request
# openid: Required for OIDC (gets ID token)
# profile: Access to name, username, etc.
# email: Access to email address
OAUTH_SCOPES=openid profile email
